# 核心异步框架
aiohttp>=3.9.0
websockets>=14.0

# LLM 与 API 调用 (确保 AsyncOpenAI 可用)
openai>=1.50.0
tenacity>=8.2.0

# 向量数据库与 RAG (ChromaDB 稳定版)
chromadb>=0.5.0
sentence_transformers>=2.2.0
numpy>=1.24.0
jieba>=0.42.1

# 视觉处理与环境配置
opencv-python>=4.8.0
pyyaml>=6.0

ollama>=0.4.0